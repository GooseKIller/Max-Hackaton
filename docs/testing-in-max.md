# Проверка в MAX

Актуальная инструкция: [deployment.md](deployment.md).

Токен команды получен. 26 сентября Антон проверил GET /me, polling и основной
диалог в MAX, исправил TLS, чтение .env, старт и подтверждение callbacks.
Код: коммит 109e847. Повторно запрашивать токен не требуется.

Это подтверждение разработки, а не гарантия постоянной доступности бота.
Финальную версию нужно проверить в mobile и web MAX.

Без токена: python backend/console.py.
С токеном в локальном .env: python backend/check_token.py проверяет /me.
python backend/runner.py запускает polling; не запускайте параллельно процессу
Антона и не используйте одновременно с webhook.

Проверка мини-приложения и запланированных напоминаний на живых устройствах
остаётся открытой. Чек-лист в [инструкции развёртывания](deployment.md).
