# Проверка версии

Дата: 26.09.2026. Результаты относятся к локальной версии, без живых токенов.
Проверки не подключались к работающему боту команды и не меняли его подписки.

| Проверка | Результат |
|---|---|
| Python tests | 168 passed локально (Python 3.13) и в итоговом Docker-образе (Python 3.12) |
| Docker, чистая сборка | 26,33 с с `--no-cache` на Docker Desktop ARM64; базовые образы уже загружены. Итоговая сборка также прошла |
| UI mobile/desktop Chromium | 20 passed; два сценария используют настоящий локальный API, остальные — контролируемые ответы для проверки состояний UI |
| Frontend | Production build и проверка форматирования прошли |
| API smoke test | Локальные UI/health/planner доступны; неверный бюджет отклоняется; webhook без авторизации закрыт |
| Compose / HTTPS-конфигурация | Compose и Caddy config validation прошли; сертификат для публичного домена не выпускался |
| Материалы | 11 слайдов PPTX и PDF; все страницы PDF просмотрены после экспорта |
| MAX mobile/web, опубликованная версия | Не проверено |
| Публичный HTTPS | Нет предоставленного адреса |
| Реальная доставка напоминания | Не проверено |
| Реальный каталог/наличие билетов | Не проверено, используется синтетический каталог |
| Интервью и эффект на траты | Не проводились в рамках этой проверки |

В Python-тестах остаётся предупреждение Starlette о будущей замене `httpx` в
тестовом клиенте; ошибок тестов нет. Время сборки относится только к указанной
машине и не гарантирует такое же время на сервере проверяющих.

Браузерные тесты выполнены в Linux Chromium с мобильным и настольным размерами
экрана. Это не проверка встроенного браузера MAX, Safari или реальных iOS/Android.
GitHub Actions запускает Python-тесты, frontend build/format и Docker build;
успешный локальный прогон сам по себе не подтверждает результат GitHub Actions.
